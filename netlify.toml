# Netlify picks this up automatically so you don't have to type build settings.
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any path (safe even though Tally has one route).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
